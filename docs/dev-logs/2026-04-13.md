# 2026-04-13

## 当天开发概括

收尾昨天的 GitHub Release 自动发布链路：修复本机 keychain 自动探测逻辑，恢复被临时验证流程污染的默认 keychain，并把 release workflow 需要的四个签名 secrets 成功写入 GitHub 仓库。

## 详细内容

### 1. 完成事项

- 更新 [`scripts/configure-release-secrets.sh`](../../scripts/configure-release-secrets.sh)，不再写死 `login.keychain-db`，而是自动从默认 keychain、login keychain 和 `~/Library/Keychains/` 下的候选文件里寻找真正包含目标代码签名 identity 的 keychain。
- 修复本机用户级 keychain 搜索列表与默认 keychain：此前临时验证流程把 `security default-keychain` 和 `list-keychains` 指向了一个已经不存在的临时 keychain，导致后续 `security` 命令和 secrets 配置脚本都找不到 `Lingjun Zeng` identity。
- 在确认 `gh auth login` 已恢复后，实际执行 [`scripts/configure-release-secrets.sh`](../../scripts/configure-release-secrets.sh) 并成功写入以下 GitHub Repo Secrets：
  - `MACOS_SIGNING_IDENTITY`
  - `MACOS_CERTIFICATE_PASSWORD`
  - `MACOS_KEYCHAIN_PASSWORD`
  - `MACOS_CERTIFICATE_P12_BASE64`

### 2. 关键决策

- `configure-release-secrets.sh` 的默认行为改成“按 identity 反查可用 keychain”，而不是要求维护者自己知道登录钥匙串的精确文件名；因为本机环境里已经出现了 `login_renamed_1.keychain-db` 这类非默认命名。
- 继续保留脚本的 `--keychain-path` 参数，允许维护者在极端场景下显式指定来源 keychain，但常规路径优先自动探测。

### 3. 遇到的问题

- 这次暴露出的真正问题不是 `gh secret set`，而是本机 keychain 状态被此前调试命令污染：默认 keychain 指向临时路径后，`security login-keychain` 和脚本里写死的 `login.keychain-db` 都会给出误导性错误。
- 另外，`security export` 是否能顺利导出 `.p12` 仍受本机钥匙串 ACL 约束；脚本已经把这件事明确成“可能弹出系统授权框，需要维护者点允许”的前提。

### 4. 后续动作

- 下一步可以直接推一个测试 tag，例如 `v0.1.0`，验证 [`.github/workflows/release.yml`](../../.github/workflows/release.yml) 能否成功构建、创建 release 并上传 signed `.zip` / `.dmg`。
- 如果 workflow 成功，再在另一台 Mac 上验证 signed release 包的安装和 Calendar / EventKit 权限表现，确认这条自动发布链路真的可用。

## 补充记录（Release 构建修复）

### 5. 完成事项

- 更新 [LaunchAtLoginController.swift](../../MeetingCountdownApp/AppShell/LaunchAtLoginController.swift) 和 [SystemCalendarAccess.swift](../../MeetingCountdownApp/SystemCalendarBridge/SystemCalendarAccess.swift)，把长期只在主线程 UI / EventKit 桥接里使用的 `SMAppService`、`EKEventStore` 存储属性标成 `nonisolated(unsafe)`，消除 GitHub Actions `Release` 构建在 Swift 6 下对异步系统 API 的“跨 actor 发送”误判。
- 更新 [ReminderAudioEngine.swift](../../MeetingCountdownApp/ReminderEngine/ReminderAudioEngine.swift)，把 `AVAudioPlayerNode.scheduleBuffer` 调整回同步 completion-handler 版本，避免 `Release` 构建把 `AVAudioPCMBuffer` 和 `AVAudioPlayerNode` 误判成跨 actor 发送。
- 在推送 `v0.1.0` 后拉取 Actions 失败日志，确认首轮发布失败根因不是 secrets 或 `gh release`，而是三处仅在 `Release` / CI 环境暴露的 Swift 6 并发编译错误。

### 6. 关键决策

- 这次修复优先采用“最小范围解除错误发送检查”的做法，而不是立刻重写 `LaunchAtLoginController`、`SystemCalendarAccess` 或 `ReminderAudioEngine` 的 actor 边界；因为这些对象在当前架构里仍然只通过主线程入口被串行使用，问题主要是新 SDK 对系统框架类型的静态分析更严格。
- `ReminderAudioEngine` 继续保留 `@MainActor` 封装，不把音频调度改成新 actor 或后台队列；当前目标只是让 CI `Release` build 和本地运行时行为重新对齐。

## 补充记录（接入诊断导出）

### 7. 完成事项

- 更新 [`SystemCalendarConnectionController.swift`](../../MeetingCountdownApp/SystemCalendarBridge/SystemCalendarConnectionController.swift)，补充“最近一次读取到的持久化日历选择”“当前已失效的旧选择”“是否保存过选择”这几个诊断字段，并把系统日历接入状态刷新、授权请求和日历选择变化都写入统一日志。
- 新增 [`CalendarConnectionDiagnosticSnapshot.swift`](../../MeetingCountdownApp/Diagnostics/CalendarConnectionDiagnosticSnapshot.swift)，把 bundle id、版本号、EventKit 权限、聚合健康状态、已保存 / 已失效 / 当前生效的日历选择，以及当前可见系统日历列表压成可复制的纯文本诊断快照。
- 更新 [`AdvancedPage.swift`](../../MeetingCountdownApp/AppShell/Settings/AdvancedPage.swift) 和 [`Presentation.swift`](../../MeetingCountdownApp/AppShell/Settings/Presentation.swift)，在设置页“高级 -> 诊断信息”里增加“复制诊断信息”入口，并补充权限状态、已保存选择、可见日历和接入调试态的只读摘要。
- 补充 [`CalendarConnectionDiagnosticSnapshotTests.swift`](../../MeetingCountdownAppTests/Diagnostics/CalendarConnectionDiagnosticSnapshotTests.swift) 和 [`SystemCalendarBridgeTests.swift`](../../MeetingCountdownAppTests/SystemCalendarBridge/SystemCalendarBridgeTests.swift)，锁定“旧日历选择失配”这类最难靠口述还原的问题现场。

### 8. 关键决策

- “权限已给但 app 仍显示未完成接入”不再只靠一句聚合文案排查；后续统一要求把权限、候选日历、持久化选择和健康状态导出成一份文本快照，再由维护者判断是权限主体问题、日历枚举为空，还是旧选择失配。
- 继续沿用系统 `OSLog` 做结构化日志，但不把“让测试用户自己打开 Console.app 找日志”作为唯一反馈方式；设置页内必须提供一个更低门槛的导出入口。

### 9. 遇到的问题

- 现有 UI 里的“未完成日历接入”其实混合了至少三类状态：未授权、没有选中日历、以及“之前选过但当前系统里已经没有这些日历”。如果没有额外诊断字段，维护者只能靠猜。
- 当前 GitHub Release 已经改成发布 signed 产物，但这并不自动排除所有接入问题；一旦用户反馈“系统设置里已授权”，仍然需要进一步看 app 实际枚举到了哪些日历，以及当前保存的选择有没有失配。

### 10. 后续动作

- 下一步可以基于这份诊断快照，再决定是否要把统一日志导出成文件，或者直接在设置页增加“复制最近日志”入口。
- 等这批改动进入下一次打包后，再让测试用户直接从设置页复制诊断文本反馈，确认 Phase 6 / 7 的接入问题能否明显缩短定位时间。

## 补充记录（Debug / Release 权限主体隔离）

### 11. 完成事项

- 根据真实用户反馈和 `/Applications/FeishuMeetingCountdown.app` 的签名检查，确认当前 GitHub Release `v0.1.0` 已经是 signed 产物，但运行时日志仍稳定报 `尚未决定是否允许访问日历`，说明问题已经从“unsigned DMG 承接不了权限”收敛成“当前运行中的 release app 没有命中正确的 TCC 授权主体”。
- 更新 [`project.yml`](../../project.yml)，把 `MARKETING_VERSION` 提升到 `0.1.1`，同时把 Debug 配置切到独立 bundle identifier：`com.luiszeng.meetingcountdown.dev`，并把 Debug 显示名改成 `Feishu Meeting Countdown Dev`。
- 新增 [`shared-bundle-id-between-debug-and-release-confuses-calendar-permission.md`](../pitfalls/shared-bundle-id-between-debug-and-release-confuses-calendar-permission.md)，把“开发版和 release 版共用 bundle id 会让 Calendar / EventKit 权限排查变形”沉淀成正式 pitfall，并从 [pitfalls/README.md](../pitfalls/README.md) 和 [index.md](../index.md) 接入。

### 12. 关键决策

- 从这次 `v0.1.1` 开始，开发版和 release 版不再共用同一个 bundle id；否则只要两边签名主体不同，macOS TCC 就可能把“系统设置里看起来已授权”和“当前运行中的 app 真实拿到的授权状态”分裂开。
- 以后 release 渠道继续保持 `com.luiszeng.meetingcountdown` 稳定不变，开发调试链路一律使用 `.dev` 后缀的独立 app 身份。

### 13. 遇到的问题

- 这次比 unsigned DMG 更隐蔽：因为 release 包本身已经是 signed 的，所以光看“签没签名”已经不足以解释权限问题；必须进一步看到“本地开发版”和“release 版”其实还在抢同一条 bundle id。
- 用户侧系统设置截图只能说明“有一个同名 app 看起来被授权了”，不能直接证明当前运行中的 release app 真命中了同一条 TCC 记录。

### 14. 后续动作

- 基于这个修复发 `v0.1.1`，让 GitHub Release 用户安装版和本地调试版彻底分离权限主体，再验证 release 包里的 Calendar / EventKit 表现。
- 如果 `v0.1.1` 后仍有个别机器复现，就继续让用户从新的“复制诊断信息”入口反馈 bundle id、授权状态和可见日历列表，避免再次退回“只看截图猜原因”。

## 补充记录（Release 资产缺少 Calendar 权限说明）

### 15. 完成事项

- 根据 `v0.1.1` 用户反馈，直接检查安装后的 `/Applications/FeishuMeetingCountdown.app/Contents/Info.plist`，确认 release 资产里缺少 `NSCalendarsFullAccessUsageDescription`；这解释了为什么点击“授予日历权限”没有弹框，且系统设置里没有对应 app 项。
- 新增显式 plist 文件 [`Config/FeishuMeetingCountdown-Info.plist`](../../Config/FeishuMeetingCountdown-Info.plist)，不再依赖 Xcode runner 自动生成最终 `Info.plist`。
- 更新 [`project.yml`](../../project.yml)，把 app target 切到 `GENERATE_INFOPLIST_FILE=NO` + `INFOPLIST_FILE`，并把 `MARKETING_VERSION` 提升到 `0.1.2`。
- 更新 [`scripts/export-release.sh`](../../scripts/export-release.sh)，在复制 `.app` 后立即校验 `NSCalendarsFullAccessUsageDescription` 是否真实存在，避免未来再次生成“能装但不能申请 Calendar 权限”的 release 资产。
- 新增 [自动生成的 Info.plist 在 release runner 上丢失 Calendar 权限说明](../pitfalls/generated-infoplist-missing-calendar-usage-description-on-release-runner.md)，把这次 CI / 本地构建环境差异导致的权限问题沉淀成正式 pitfall。

### 16. 关键决策

- 所有直接影响 TCC 系统权限的 usage description，不再只通过 `INFOPLIST_KEY_*` build setting 注入；以后统一以显式 `Info.plist` 为准。
- release 导出脚本不仅要负责“构建成功”，还要对最终 `.app` 做关键权限字段校验；否则 CI 绿了也不代表用户能正常使用系统权限。

### 17. 遇到的问题

- 这次问题比 bundle id 混淆更隐蔽：本地 `build/manual-release` 产物包含 `NSCalendarsFullAccessUsageDescription`，但 GitHub Release 下载下来的安装包没有，说明不能只根据开发机本地构建结果判断最终 release 资产是正确的。
- 由于 release runner 使用的 Xcode / SDK 版本与本地不同，自动生成 `Info.plist` 的行为出现了不一致；如果没有直接检查安装后的 `Info.plist`，很容易把问题误判成 TCC 或 EventKit API 本身。

### 18. 后续动作

- 基于这个修复发布 `v0.1.2`，重新验证下载后的 `.dmg` 在另一台机器上是否能正常出现 Calendar 权限弹窗和系统设置项。
- 如果 `v0.1.2` 验证通过，再把“Debug / Release bundle id 隔离”和“显式 Info.plist + release 校验”视为当前 Phase 6 分发链路的稳定基线。

## 补充记录（设置页紧凑布局重排）

### 19. 完成事项

- 更新 [`MeetingCountdownApp/AppShell/SettingsView.swift`](../../MeetingCountdownApp/AppShell/SettingsView.swift) 和 [`MeetingCountdownApp/AppShell/Settings/Header.swift`](../../MeetingCountdownApp/AppShell/Settings/Header.swift)，把设置窗口全局壳层收紧到更适合 `680x704` 实际窗口的节奏，并移除原来跨 tab 常驻的右上大状态卡，只保留标题、说明和核心 badge。
- 更新 [`MeetingCountdownApp/AppShell/Settings/CalendarPage.swift`](../../MeetingCountdownApp/AppShell/Settings/CalendarPage.swift)、[`MeetingCountdownApp/AppShell/Settings/RemindersPage.swift`](../../MeetingCountdownApp/AppShell/Settings/RemindersPage.swift)、[`MeetingCountdownApp/AppShell/Settings/AudioPage.swift`](../../MeetingCountdownApp/AppShell/Settings/AudioPage.swift) 和 [`MeetingCountdownApp/AppShell/Settings/AdvancedPage.swift`](../../MeetingCountdownApp/AppShell/Settings/AdvancedPage.swift)，把原本容易在窄窗下留下大片空白的“短侧栏 + 长主栏”结构改成顺序式单栏内容流；只有概览页和少数内部头部在足够宽时才恢复双栏。
- 更新 [`MeetingCountdownApp/AppShell/Settings/OverviewPage.swift`](../../MeetingCountdownApp/AppShell/Settings/OverviewPage.swift) 和 [`MeetingCountdownApp/AppShell/Settings/Components.swift`](../../MeetingCountdownApp/AppShell/Settings/Components.swift)，收紧摘要卡最小高度，并为 `infoRow`、开关行补上横排 / 竖排自适应，减少窄宽度下的压缩和无效留白。
- 本地执行 `xcodebuild -project MeetingCountdown.xcodeproj -scheme FeishuMeetingCountdown -configuration Debug build CODE_SIGNING_ALLOWED=NO`，确认这轮 SwiftUI 排版改动可正常编译通过。

### 20. 关键决策

- 这次不采用瀑布流或自动补洞式布局，因为设置页本质上是状态扫描和配置页，不是信息卡片墙；真正的问题是窄窗下仍在勉强维持桌面双栏骨架。
- 从这版开始，把 `680x704` 视为设置页的真实基线尺寸，而不是“只在默认宽窗里好看、缩小后再尽量不坏”；因此非概览页默认优先单栏，宽模式只作为额外加分，而不是基础依赖。
- 全局头部不再承担过多状态摘要职责；“下一场会议 / 提醒状态 / 当前音频”这些更适合留在具体 tab 内，避免用户每次切到设置页都先被重复信息占掉首屏。

### 21. 遇到的问题

- 现有 `ViewThatFits` 在很多地方虽然看起来像响应式，但因为横向版本没有显式“舒适宽度”门槛，SwiftUI 会持续压缩内容，导致页面在视觉上还维持双栏，实际却只剩一边在长、一边在空。
- 语言、诊断动作和开关行这类局部组件，原先也默认按横排写死；即使整页改成单栏，如果这些局部头部继续硬横排，窄窗下仍会出现挤压和奇怪留白，所以这一轮也顺手补了组件级回退布局。

### 22. 后续动作

- 下一步应该直接在真实窗口里再跑一轮视觉检查，重点看概览页 hero、日历列表在两列阈值附近的观感，以及诊断区按钮在中英文切换后的稳定性。
- 如果这轮布局方向成立，再继续做第二轮 polish：统一 page intro 层级、进一步压缩音频页条目间距，并检查 `GlassPanel` / `GlassCard` 的 padding 是否还能再收一点。

## 补充记录（概览页改成状态面板）

### 23. 完成事项

- 更新 [`MeetingCountdownApp/AppShell/Settings/Header.swift`](../../MeetingCountdownApp/AppShell/Settings/Header.swift) 和 [`MeetingCountdownApp/AppShell/Settings/Presentation.swift`](../../MeetingCountdownApp/AppShell/Settings/Presentation.swift)，把设置窗口顶部标题区改成真正的状态摘要卡：标题切到“会议提醒概览”，副标题改成日历连接 / 提醒 / 音频 / 同步健康度的系统视角描述，状态 badge 也从“CalDAV 单一路径 / 已授权 / 正常”改成更像运行态的 `运行正常 / CalDAV 已连接 / 已授权访问日历 / x 分钟前同步成功`。
- 重写 [`MeetingCountdownApp/AppShell/Settings/OverviewPage.swift`](../../MeetingCountdownApp/AppShell/Settings/OverviewPage.swift)，把首页结构改成“下一场会议主卡 -> 提醒 / 同步双卡 -> 四宫格状态卡 -> 最近问题”这条清晰的数据流，不再使用“先看提醒是不是正常”“这四项最值得先看”这类设计者口吻文案。
- 更新概览相关展示文案：例如把 `09:59 触发，倒计时 10 秒` 改成更明确的 `将在会议开始前 10 秒触发提醒，倒计时持续 10 秒`；把 `一切正常，正在等待下一场会议` 改成更结果导向的 `当前无异常，下一场会议将按计划提醒`。
- 本地再次执行 `xcodebuild -project MeetingCountdown.xcodeproj -scheme FeishuMeetingCountdown -configuration Debug build CODE_SIGNING_ALLOWED=NO`，确认这轮结构重排与文案调整都可正常编译通过。

### 24. 关键决策

- 这次不继续把概览页当“设置首页”，而是明确把它当作 dashboard / status board；因此标题、主卡和四宫格都优先表达“系统现在会做什么”，而不是“用户应该去哪里看什么”。
- 文案统一遵循“描述事实，不描述设计意图；描述系统行为，不描述界面目的”这条原则。能直接说“下一场会议将在何时提醒”的地方，不再说“这里会显示提醒状态”。
- 下一场会议主卡里不再把时间说明压缩成模糊的一句短文，而是拆成开始时间、距离开始还有多久、提醒会如何触发三层事实，让用户更快确认“这场会到底会不会按计划提醒”。

### 25. 遇到的问题

- 当前提醒的真实触发时间往往是“会议开始前若干秒”，但原来统一走 `DateFormatter.timeStyle = .short` 时很容易把 `09:59:50` 压成 `09:59`，让文案看起来像“会前 1 分钟触发”。这次改成“会前 x 秒 / x 分钟触发提醒”的说法，就是为了避免这种时间粒度误导。
- 顶部 header 原先沿用“设置页标题 + badge”骨架时，虽然已经比最初版本紧凑，但仍然更像一个 form shell，不像状态面板；因此这轮干脆把 header 也并入概览式摘要卡，而不是继续在文案层面硬补。

### 26. 后续动作

- 下一步可以直接在真实数据下检查概览页的三个关键动作：`加入会议`、`查看详情` 和 `立即同步` 是否都符合直觉，尤其是没有视频会议链接时的退化行为是否足够自然。
- 如果这版方向稳定，再继续统一其他 tab 的低频说明文案，把剩余的“这里会显示…”、“控制…” 这类设计说明腔也逐步替换成状态 / 结果导向表达。

## 补充记录（概览页双卡阈值再收紧）

### 27. 完成事项

- 更新 [`MeetingCountdownApp/AppShell/Settings/OverviewPage.swift`](../../MeetingCountdownApp/AppShell/Settings/OverviewPage.swift)，把概览页“提醒状态 / 同步状态”这组双卡的横向保持阈值从 `620` 继续下调到 `560`，让约 `680x704` 的真实设置窗口更容易维持并排展示，减少无效留白和过早单栏回退。

### 28. 关键决策

- 这次只收紧响应式断点，不改卡片样式、内容密度和按钮结构；目标是延续当前概览页节奏，只解决“在真实窗口里太早折叠”的问题。
