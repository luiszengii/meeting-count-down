# 2026-04-15

## 当天开发概括

把 app 版本从 `0.1.2` 提升到 `0.1.3`，重新校验工程、测试和 tag 驱动的 GitHub Release 发布链路，为新一版正式分发做准备。

## 详细内容

### 1. 完成事项

- 更新 [`project.yml`](../../project.yml) 和 [`MeetingCountdown.xcodeproj/project.pbxproj`](../../MeetingCountdown.xcodeproj/project.pbxproj)，把 `MARKETING_VERSION` 从 `0.1.2` 提升到 `0.1.3`，保持项目源配置和当前工程内版本一致。
- 更新 [`docs/dev-logs/README.md`](./README.md) 与 [`docs/index.md`](../index.md)，把今天的版本发布记录接入正式文档索引，避免 release 版本演进只留在 Git tag 和聊天记录里。
- 重新执行 `xcodegen generate` 和 `xcodebuild test -project MeetingCountdown.xcodeproj -scheme FeishuMeetingCountdown -destination 'platform=macOS'`，确认升版后工程与测试仍稳定。
- 在 [`.github/workflows/release.yml`](../../.github/workflows/release.yml) 中把 `actions/checkout` 从 `v4` 升级到 `v5`，提前消除 GitHub Actions 关于 Node 20 runtime 即将退场的告警，避免后续由托管 runner 默认切换到 Node 24 时再临时补救。

### 2. 关键决策

- 继续沿用仓库现有的正式发布路径：先提交版本变更，再推送 `v0.1.3` tag，由 [`.github/workflows/release.yml`](../../.github/workflows/release.yml) 触发 signed release workflow，而不是在本机直接上传 unsigned 资产。
- 版本源仍以 [`project.yml`](../../project.yml) 为准；任何手工发布 tag 都必须与 `MARKETING_VERSION` 保持完全一致，避免 GitHub Release 版本和 app 内版本再次漂移。

### 3. 遇到的问题

- 当前仓库的 release 脚本会强校验“工作区干净 + tag 已存在 + `project.yml` 版本一致”，因此升版动作必须先落 commit，再打 tag，不能跳过版本提交直接创建 release。
- 文档系统同样需要跟上版本变更；如果只改工程版本而不补日志索引，后续维护者很难从正式文档里追溯 `0.1.3` 是在什么上下文下发布的。

### 4. 后续动作

- 推送版本提交和 `v0.1.3` tag，观察 GitHub Actions 的 Release workflow 是否成功创建新 release 并上传 signed `.zip` / `.dmg`。
- 如果 workflow 完成，再抽样检查 GitHub Release 页面上的版本号、release note 和资产命名是否都与 `0.1.3` 一致。
- 继续留意 GitHub 官方对基础 action runtime 的迁移节奏；当前仓库只用到了 `actions/checkout`，因此先完成 `v5` 升级即可，不必为了这条告警改动本机 Node 环境。
